# VibedTracker Server Configuration
# Kopiere diese Datei zu .env und setze sichere Werte!

# ===========================================
# PFLICHT - Diese Werte MÜSSEN gesetzt werden
# ===========================================

# Datenbank Passwort (sicheres Passwort wählen!)
# Generieren mit: openssl rand -base64 24
DB_PASSWORD=change-me-secure-password-here

# JWT Secret (mindestens 32 Zeichen, zufällig generieren!)
# Generieren mit: openssl rand -base64 32
JWT_SECRET=change-me-very-long-and-secure-jwt-secret-key

# Admin Account (wird beim ersten Start erstellt)
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=change-me-secure-admin-password

# ===========================================
# OPTIONAL
# ===========================================

# Registrierung erlauben (default: true)
# ALLOW_REGISTRATION=true

# API Port (default: 8080, nur für Development relevant)
# PORT=8080
